/**
 * TOPIC: ad hoc, constructive, greedy
 * status:
 */
#include <bits/stdc++.h>

std::istream &is = std::cin;
std::ostream &os = std::cout;
using i64= std::int64_t;

void solve( int cs ) {
    int i,j,k,n;
    is >> n;
    std::vector<i64> x(n-1);
    for ( auto &v: x ) is >> v;

}

int main() {
    int i, j, k, cs = 0, ts;
    for ( is >> ts; ts--; solve(++cs) ) ;
    return 0;
}   
 